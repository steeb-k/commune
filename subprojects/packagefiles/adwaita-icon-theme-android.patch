diff --git a/meson.build b/meson.build
index 37cc391..4b79dc9 100644
--- a/meson.build
+++ b/meson.build
@@ -24,7 +24,7 @@ install_subdir(
 )
 
 files('Adwaita/scalable/places/folder.svg')
-if host_machine.system() == 'windows'
+if host_machine.system() in ['windows', 'android']
   install_data(
     'Adwaita/scalable/places/folder.svg',
     install_dir: adwaita_dir / 'scalable' / 'status',
@@ -42,7 +42,12 @@ endif
 
 command_output = run_command('tools/find_cursors_win32.py', check: true)
 win32_cursors = command_output.stdout().strip().split('\n')
-if host_machine.system() == 'windows'
+if host_machine.system() == 'android'
+  # Android draws no pointer, so every cursor here is weight with no
+  # reader: 16 MB of the 19 MB this project installs, and fifty-odd
+  # symlinks along with it.
+  message('Android: not installing cursors')
+elif host_machine.system() == 'windows'
   cursors_with_rel_path = []
   icons_dir = get_option('datadir') / 'icons'
   foreach cursor : win32_cursors
@@ -107,14 +112,16 @@ else
   endforeach
 endif
 
-gtk_update_icon_cache = find_program(
-  'gtk4-update-icon-cache',
-  'gtk-update-icon-cache',
-  required : true,
-)
-meson.add_install_script(
-  gtk_update_icon_cache,
-  '-qtf',
-  get_option('prefix') / adwaita_dir,
-  skip_if_destdir: true,
-)
+if host_machine.system() != 'android'
+  gtk_update_icon_cache = find_program(
+    'gtk4-update-icon-cache',
+    'gtk-update-icon-cache',
+    required : true,
+  )
+  meson.add_install_script(
+    gtk_update_icon_cache,
+    '-qtf',
+    get_option('prefix') / adwaita_dir,
+    skip_if_destdir: true,
+  )
+endif
